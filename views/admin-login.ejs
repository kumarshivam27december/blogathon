<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        /* Star Twinkling Background */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background: radial-gradient(ellipse at bottom, #1b2735 0%, #090a0f 100%);
            color: rgb(157, 51, 51);
            overflow: hidden;
            min-height: 100vh;
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: center; /* Center content vertically */
            align-items: center; /* Center content horizontally */
        }

        /* Stars */
        .stars {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .star {
            position: absolute;
            background: white;
            border-radius: 50%;
            animation: twinkle linear infinite;
        }

        /* Star Sizes and Animation Durations */
        .star.small {
            width: 1px;
            height: 1px;
            animation-duration: 3s;
        }

        .star.medium {
            width: 2px;
            height: 2px;
            animation-duration: 5s;
        }

        .star.large {
            width: 3px;
            height: 3px;
            animation-duration: 7s;
        }

        @keyframes twinkle {
            0%, 100% {
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
        }

        /* Navbar */
        .navbar {
            background: rgba(0, 0, 0, 0.7);
            padding: 20px 0;
            position: fixed; /* Fix navbar at the top */
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Logo in Red */
        .logo a {
            color: red; /* Red color for the logo */
            font-size: 24px;
            font-weight: bold;
            text-decoration: none;
        }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 40px; /* Increased gap between tabs */
            margin: 0;
            padding: 0;
        }

        .nav-links li a {
            color: white;
            text-decoration: none;
            font-size: 16px;
            transition: all 0.3s ease; /* Smooth transition for hover effect */
        }

        /* Double underline in red on hover */
        .nav-links li a:hover {
            text-decoration: double underline red; /* Double underline in red */
        }

        /* Form Section */
        .section {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px; /* Reduced padding */
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            max-width: 400px; /* Smaller width */
            width: 100%;
            position: relative;
            z-index: 1;
            margin-top: 100px; /* Add more margin to avoid overlap with navbar */
            margin-bottom: 40px; /* Add margin to avoid overlap with footer */
            display: flex;
            flex-direction: column;
            align-items: center; /* Center form content horizontally */
        }

        .container h2 {
            text-align: center;
            margin-bottom: 20px;
            color: white;
        }

        .container h3 {
            text-align: center;
            margin-bottom: 20px;
            color: white;
            font-size: 18px;
        }

        .form-group {
            margin-bottom: 15px; /* Reduced margin */
            width: 100%; /* Make form group full width */
        }

        .form-group label {
            font-weight: bold;
            margin-bottom: 5px;
            display: block;
            color: white;
        }

        .form-group input[type="text"],
        .form-group input[type="password"] {
            width: 100%; /* Full width */
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            box-sizing: border-box; /* Ensure padding doesn't affect width */
        }

        .btn {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            width: 100%; /* Full-width button */
            box-sizing: border-box; /* Ensure padding doesn't affect width */
        }

        .btn:hover {
            background-color: #0056b3;
        }

        /* Footer */
        .footer {
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 20px 0;
            width: 100%;
            position: relative; /* Remove fixed positioning */
            z-index: 1;
            margin-top: auto; /* Push footer to the bottom */
        }

        .footer-content {
            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .footer-section {
            flex: 1;
            margin: 10px 20px;
        }

        .footer-section h3 {
            margin-bottom: 10px;
        }

        .footer-section p {
            margin: 5px 0;
        }

        .footer-section ul {
            list-style: none;
            padding: 0;
        }

        .footer-section ul li a {
            color: white; /* White color for Quick Links */
            text-decoration: none;
            font-size: 16px;
        }

        .footer-section ul li a:hover {
            text-decoration: underline; /* Underline on hover */
        }

        .social-links a {
            margin-right: 10px;
            font-size: 20px;
            color: white;
            text-decoration: none;
        }

        .footer-bottom {
            text-align: center;
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px solid #444;
        }
    </style>
</head>
<body>
    <!-- Star Twinkling Background -->
    <div class="stars">
        <!-- Stars are dynamically generated using JavaScript -->
    </div>

    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="logo">
                <a href="/">Pradyut Foundation</a>
            </div>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/about">About</a></li>
                <li><a href="/rules">Rules</a></li>
                <li><a href="/contact">Contact</a></li>
                <li><a href="/register">Register</a></li>
                <li><a href="/admin/login">Admin Login</a></li>
            </ul>
        </div>
    </nav>

    <!-- Admin Login Form -->
    <section class="section">
        <div class="container">
            <h2>Admin Login</h2>
            <h4>Type Till Sunrise Admin Panel</h4>
            <form class="registration-form" action="/admin/login" method="POST">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit" class="btn">Login</button>
            </form>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="footer-section">
                <h3>Contact Us</h3>
                <p><i class="fas fa-envelope"></i> management.pradyut@gmail.com</p>
                <p><i class="fas fa-phone"></i> +91 756 995 2369, +91 760 999 2754</p>
                <p><i class="fas fa-map-marker-alt"></i> LPU Campus, Punjab</p>
            </div>
            <div class="footer-section">
                <h3>Follow Us</h3>
                <div class="social-links">
                    <a href="https://www.instagram.com/pradyut_foundation21"><i class="fab fa-instagram"></i></a>
                    <a href="https://www.linkedin.com/company/pradyut-foundation/"><i class="fab fa-linkedin"></i></a>
                    <a href="https://www.facebook.com/pradyutfoundation/"><i class="fab fa-facebook"></i></a>
                </div>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul>
                    <li><a href="/register">Registration</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                    <li><a href="/rules">Rules</a></li>
                    <li><a href="/admin/login">Admin Panel</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>© 2024 Type Till Sunrise - Inter-College Blogathon</p>
        </div>
    </footer>

    <script>
        // JavaScript to generate stars dynamically
        const starsContainer = document.querySelector('.stars');

        function createStar() {
            const star = document.createElement('div');
            star.classList.add('star');

            // Randomly assign star size
            const sizes = ['small', 'medium', 'large'];
            const size = sizes[Math.floor(Math.random() * sizes.length)];
            star.classList.add(size);

            // Randomly position stars
            star.style.top = `${Math.random() * 100}%`;
            star.style.left = `${Math.random() * 100}%`;

            // Randomize animation delay
            star.style.animationDelay = `${Math.random() * 5}s`;

            starsContainer.appendChild(star);
        }

        // Generate 200 stars
        for (let i = 0; i < 200; i++) {
            createStar();
        }
    </script>
</body>
</html>